import React, { FC } from 'react'
// TYPES

// CONSTANTS

// DEFAULT FUNCTIONS

// TODO: Change all TextFields which wrapped in <Control/> tag (in modal forms) to this smart component

interface IControlTextFieldProp {
  // You should declare props like this, delete this if you don't need props
  someProp: string
}

const ControlTextField: FC<IControlTextFieldProp> = () => {
  return <></>
}

export default ControlTextField
